body copy_from the_hub
{
    servers => { "$(sys.policy_hub)"};
    source => "/var/cfengine/masterfiles/promises.cf";
    compare => "digest";
}

bundle agent main
{
    files:
        "/tmp/promises.cf"
            create => "true",
            copy_from => the_hub;
}
